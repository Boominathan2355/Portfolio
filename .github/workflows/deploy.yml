name: Disabled Workflow

on:
  # This workflow is disabled to prevent conflicts with manual 'npm run deploy'.
  # It is kept here as a placeholder. You can safely delete this file.
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Manual deployment is enabled via 'npm run deploy'. This Action is currently inactive."
